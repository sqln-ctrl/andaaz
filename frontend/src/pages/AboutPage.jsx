import { T } from "../config/theme";

export default function AboutPage() {
  return (
    <div style={{ paddingTop: 100, minHeight: "100vh", background: T.bg }}>
       {/* ... (Keep the exact same AboutPage JSX content here) ... */}
    </div>
  );
}